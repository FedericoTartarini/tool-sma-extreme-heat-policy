name: Python Testing 🐍
on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build SMA
        run: |-
          python -m pip install --upgrade pip
          pip install pipenv
          pipenv install --dev

      - name: Ruff Check
        run: |-
          pipenv run ruff check . --output-format=github

      - name: Ruff Format (verify)
        run: |-
          pipenv run ruff format --check .

      - name: Install Playwright and browsers
        run: |-
          pipenv run playwright install --with-deps

      - name: Start Server
        run: |-
          pipenv run python main.py &
          sleep 5

      - name: Test SMA
        run: |-
          pipenv run python -m pytest --base-url="http://0.0.0.0:8080"
